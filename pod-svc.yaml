apiVersion: v1
kind : Pod 
metadata: 
   name: pods-for-svc
   labels:  #manadatory for defining labels 
      group: svc-pod
    
spec: 
   containers: 
     - name: cont1 
       image: nginx   
       ports: 
          - name: http
            containerPort: 80
          - name: https 
            containerPort: 443  
